<div class="container">
  <div class="row">
    <h1>Products</h1>
    <% @product.each do |product| %>
      <div class="col-sm-6 col-md-4">
        <div class="thumbnail">
          <%= link_to image_tag(product.photo_url), product_path(product) %>
          <div class="caption">
            <h3><%= link_to product.name, product_path(teddy) %></h3>
            <p><%= product.category.name %></p>
            <p>$$$</p>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
